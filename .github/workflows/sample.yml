name: Shell Commands
on: [push]
jobs:
  run-shell-commands:
    runs-on: ubuntu:18.04
    steps:
      - name: string
        run: echo "Welcome to SP SOFTWARE SOLUTIONS"
      - name: multiline script
        run: |
          node -v
          npm -v
