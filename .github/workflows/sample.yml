name: Shell Commands
on: [push]
jobs:
  run-shell-commands:
    run-on: ubuntu:latest
    steps:
      - name: string
        run: echo "Welcome to SP SOFTWARE SOLUTIONS"
      - name: multiline script
        run: |
          node -v
          npm -v
